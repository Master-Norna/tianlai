# 安全策略

## 支持范围

当前 `0.5.x` 系列接受安全修复。更早的实验版本只在修复能够安全
回移时处理。

## 报告问题

若托管平台已启用私密漏洞报告，请优先使用该入口。否则只提交一条不包含利用代码、
私人文件、音源或受保护乐谱的最小公开 issue，请求建立私密沟通渠道。

报告最好包含受影响版本、平台、入口、预期边界、实际行为和最小复现条件。不要在
修复前公开可读取任意本机文件、覆盖任意路径、执行任意代码、资源包投毒或拒绝
服务的完整操作步骤。

## 当前安全边界

- MCP 本地导谱只读取项目/乐谱目录及 `TIANLAI_INPUT_ROOTS` 显式开放的目录；
- 外部路径在解析符号链接或 Windows reparse/junction 后再做包含检查；
- score、MIDI、MusicXML/MXL、渲染时长、音符数、执行器、内存和输出大小均有
  默认上限；
- 可信清单缺失或无效时，`trusted_only=true` 失败关闭；
- 候选与导入工程默认不可覆盖；显式候选覆盖同时绑定旧回执和准备阶段的候选
  清单，并在目录交换前后递归复验绑定产物；
- 音源安装器应固定版本和摘要，在临时目录校验完成后再发布到正式资源目录。

普通本地 CLI 允许用户显式选择自己的输入路径；它不等于 MCP Agent 获得整机文件
读取权限。天籁是离线渲染器，不应以管理员权限运行。
