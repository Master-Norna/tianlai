# 合成铜管

默认 `synth_brass`（引擎 `1.0.0`，`formal`）以四声部带限锯齿/脉冲模拟簧片式激励，加入极短音高冲击、力度敏感滤波包络和较强软饱和，形成合成铜管的“喷口”起音。

- 校准音域：MIDI 36–96，硬边界。
- 控制：力度、`expression`、`modulation`、延音踏板。
- 固定种子：`2236067977`。
- 试听谱例：`examples/合成铜管_程序合成.events.json`。
- 状态：协奏/长时语境待审，不声称替代真实铜管采样。

显式应急回退：GeneralUser GS bank `0` / program `62`；不静默降级。

