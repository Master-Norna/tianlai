# 雨境合成氛围

`formal` 程序雨境：宽带雨幕、低频屋面/远场成分与按固定种子调度的立体声雨滴谐振构成无接缝持续氛围。

- `modulation` 控制雨滴密度，力度/`expression` 控制雨势，`distance` 控制距离衰减。
- `note_on` 开始雨境，`note_off` 长淡出；事件相同即可逐样本重复。
- 当前绑定版本的单音色/单场景试听已通过，因此为 `formal`；尚未区分窗面、树叶、车顶和地面积水，也未加入雷声或实录空间响应，长循环协奏/长时语境仍待审。
