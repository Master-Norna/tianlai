# 参与贡献

天籁欢迎代码、协议、文档、测试、音源线索和听审反馈。提交内容应帮助创作者获得
更可控、可复算、可继续编辑的音乐执行流程，而不是把审美决定隐藏进不可解释的
默认值。

公开仓库中的“音色、音域或协奏问题”模板会要求乐器 ID、候选秒数、分轨范围和
可复算证据；报告者不需要是乐器专家，也不需要把一次听感上升为全面质量结论。
程序错误使用独立模板。两类报告都不要附带无权公开的曲谱、录音或采样。

## 开发环境

```cmd
安装运行环境.cmd -SkipSmoke
```

随后运行：

```powershell
.\.venv\Scripts\python.exe -m pip install -e ".[mcp,dev]"
.\.venv\Scripts\python.exe -m pytest -q -m "not external_assets and not listening"
```

Windows 10/11 x64 是当前参考平台；协议和纯 Python 路径也应保持 Linux 可用。
新增文件名可使用中文，但 Python 模块、命令名、JSON 字段和稳定 ID 应使用 ASCII
英文。所有路径操作使用 `pathlib`，不得写死个人磁盘路径。

默认门禁是无需大型音源的完整 portable 集合。改动真实采样映射时，再在资源已经
完整安装的环境运行 `-m external_assets`；改动冻结试听协议或生产试听证据时运行
`-m listening`。资源完全不存在可以跳过，但出现部分安装、摘要不符或许可实物
不符必须失败，不能用 skip 掩盖损坏状态。

## 变更原则

- 资源缺失、许可不明、音域不满足或语义无法保留时明确失败或报告，不静默换源；
- Agent 给出的配器、混音和编辑建议默认不可执行，创作者确认后才进入正式输入；
- 改动 score v1 已有音符时保留 `event_id`，新增音符使用新的唯一 ID；
- 新协议需要版本字段、严格解析、边界测试和迁移说明；
- 音频优化同时提交可复算测试或听审材料的生成方法，不把“感觉更好”写成无边界
  的全局事实；
- 不提交 `.venv/`、`音源/`、用户乐谱、渲染输出或本机缓存。

## 第三方材料

不要在 issue 或贡献中直接上传来源不清、抓取自聚合站或未经授权的音频、乐谱、
MIDI、MusicXML、图片或代码。音源候选至少要记录官方来源、版本、许可证原文、
署名要求、文件摘要和允许的使用/再分发方式。项目非商用不会降低这条门槛。

项目自研贡献按 Apache-2.0 提交；提交者应有权提供相关内容。第三方资源不会因为
进入天籁清单而改用 Apache-2.0，仍按自己的许可证管理。

## 提交与评审

提交信息用一句话概括结果。Pull Request 应说明：

1. 解决了什么问题；
2. 用户可观察到什么变化；
3. 运行了哪些测试或听审；
4. 是否改变协议、默认值、音频、资源或许可边界；
5. 是否需要迁移已有工程。

原始项目署名见 `NOTICE`；修改版应按 Apache-2.0 醒目标明自己的修改。
